# GBDK build
obj/

*.o *.lst *.map *.ihx *.sym *.cdb *.adb *.asm *.noi *.rst *.d

# Downloaded toolchain (optional: remove to ignore)
gbdk-linux64.tar.gz

# SameBoy vendored build deps (SDL2 source/build, RGBDS tarball)
emu/.deps/
